<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Hackitout" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryhead</item>
        <item name="colorPrimaryDark">@color/primaryhead</item>


        <!-- Status bar color. -->
        <item name="statusBarBackground">@color/primaryhead</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="tileStyle">
        <item name="android:background">@drawable/my_border</item>
        <item name="android:textColor">@color/font</item>
        <item name="android:gravity"> center_vertical</item>
        <item name="android:padding">16dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_height">@dimen/list_item_height</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">18sp</item>
    </style>

</resources>